# Задачи локальным агентам: tool-code-execution

Правило: один открытый пункт за цикл. Статус меняй только после проверки.

## Открыто

- [ ] T-001 Жёсткий timeout исполнения (`timeout_sec`) через отдельный процесс/сигнал, не только «hint» в схеме. Критерий: сниппет `while True: pass` завершается за ≤ timeout+1с, процесс сервера жив.
- [ ] T-002 Лимит памяти на сниппет (cgroup уже на контейнере; внутри — `resource` или отдельный worker). Критерий: тест на большой `list` не валит HTTP-процесс.
- [ ] T-003 Аудит разрешённых модулей: таблица в DESIGN + тест на каждый запрещённый (`os`, `subprocess`, `socket`, `ctypes`).
- [ ] T-004 `/v1/invoke` не принимает поле `code` длиннее лимита с кодом 400 без traceback наружу в проде (`TOOL_DEBUG=0`).
- [ ] T-005 Dockerfile: multi-stage не нужен, но `.dockerignore` обязателен (`.git`, `.venv`, tests опционально в runtime).
- [ ] T-006 Контрактный тест HTTP (`healthz`/`schema`/`invoke`) на `http.client` без внешних пакетов.
- [ ] T-007 Документировать, когда оставлять server-side `code_execution` xAI (нет — по умолчанию локально).

## Закрыто

- [x] T-000 Каркас сервера, AST-guard, pytest на арифметику и `import os`.

